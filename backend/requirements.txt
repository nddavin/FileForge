# Core Framework
aiofiles>=24.1.0

# Synchronized with pyproject.toml (backend-focused)

# Core Framework
fastapi>=0.115.6
uvicorn[standard]>=0.32.0

# Database & Migrations
sqlalchemy>=2.0.36
alembic>=1.14.0
psycopg2-binary>=2.9.10

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pydantic>=2.10.3
pydantic-settings>=2.0.0

# Async HTTP
httpx>=0.28.1
python-multipart>=0.0.21

# Task Queue / Cache
redis>=5.2.1
celery>=5.4.0

# Supabase
supabase>=2.27.2

# Utilities
python-dotenv>=1.0.1
python-dateutil>=2.8.2
mutagen>=1.47.0
feedparser>=6.0.0

# Testing / Dev
pytest>=8.3.4
pytest-asyncio>=0.25.3
pytest-cov>=6.0.0

# Notes:
# - Heavy optional deps (torch, torchaudio, opencv, etc.) intentionally excluded here;
#   install them in a separate extras/virtualenv when needed.
aiofiles>=24.1.0
