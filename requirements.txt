# Top-level requirements file
# Project-specific requirements are delegated to service folders

# Backend Core Dependencies
-r backend/requirements.txt

# Optional: ML/AI Dependencies (uncomment if needed)
# -r backend/requirements-ml.txt

# Frontend Dependencies
# The frontend uses Node.js/npm - see frontend/package.json
# Install with: cd frontend && npm install
